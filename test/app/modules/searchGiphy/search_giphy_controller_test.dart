import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:giphys/app/modules/searchGiphy/search_giphy_controller.dart';
import 'package:giphys/app/modules/searchGiphy/search_giphy_module.dart';

void main() {
  initModule(SearchGiphyModule());
  // SearchGiphyController searchgiphy;
  //
  setUp(() {
    //     searchgiphy = SearchGiphyModule.to.get<SearchGiphyController>();
  });

  group('SearchGiphyController Test', () {
    //   test("First Test", () {
    //     expect(searchgiphy, isInstanceOf<SearchGiphyController>());
    //   });

    //   test("Set Value", () {
    //     expect(searchgiphy.value, equals(0));
    //     searchgiphy.increment();
    //     expect(searchgiphy.value, equals(1));
    //   });
  });
}
