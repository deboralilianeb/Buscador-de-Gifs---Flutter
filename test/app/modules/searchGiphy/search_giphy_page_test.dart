import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:giphys/app/modules/searchGiphy/search_giphy_page.dart';

void main() {
  testWidgets('SearchGiphyPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SearchGiphyPage(title: 'SearchGiphy')));
    //  final titleFinder = find.text('SearchGiphy');
    //  expect(titleFinder, findsOneWidget);
  });
}
